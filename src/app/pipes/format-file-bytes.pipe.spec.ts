import { FormatFileBytesPipe } from './format-file-bytes.pipe';

describe('FormatFileBytesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFileBytesPipe();
    expect(pipe).toBeTruthy();
  });
});
