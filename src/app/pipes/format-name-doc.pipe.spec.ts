import { FormatNameDocPipe } from './format-name-doc.pipe';

describe('FormatNameDocPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNameDocPipe();
    expect(pipe).toBeTruthy();
  });
});
